/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` block at build time. See the
// StoreFooter for where these surface.
declare const __APP_COMMIT__: string;
declare const __APP_BUILD_DATE__: string;
